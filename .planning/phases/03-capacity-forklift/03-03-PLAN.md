---
phase: 03-capacity-forklift
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "User sees running total pallet count updating as elements are placed/removed"
    - "Total count includes floor pallets, stacked pallets (respects ceiling height), and shelf pallets"
    - "User can drive forklift with arrow keys to test aisle navigation"
    - "Forklift stops when colliding with walls, obstacles, or racks (cannot drive through)"
    - "User receives visual feedback when forklift is blocked (collision highlight)"
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification of Phase 3: Capacity and Forklift features working together.

Purpose: Confirm all phase success criteria are met before marking Phase 3 complete.
Output: Phase checkpoint approval or issue list for gap closure.
</objective>

<execution_context>
@/Users/juergs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juergs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 3 features: reactive capacity counter (CAP-01, CAP-02, CAP-03) and forklift driving with collision detection (FORK-01, FORK-02, FORK-03).
  </what-built>
  <how-to-verify>
    Open index.html in browser and test:

    **Capacity Counter (sidebar top):**
    1. Verify "Capacity" section appears at top of sidebar with "Total: 0 pallets"
    2. Drag a Rack onto canvas -- total updates to 6 (3 levels x 2 pallets)
    3. Drag a Pallet onto canvas -- total updates to 9 (6 rack + 3 stacked at 144" ceiling / 48" pallet)
    4. Change ceiling height input to 96 -- total updates to 8 (6 rack + 2 stacked)
    5. Delete the rack (select + Delete key) -- total drops to 2
    6. Delete the pallet -- total returns to 0

    **Forklift Driving:**
    7. Drag a Forklift onto canvas
    8. Press arrow keys -- forklift moves smoothly in all directions
    9. Place a Wall near forklift, drive into it -- forklift stops, turns red, wall gets red outline
    10. Drive away from wall -- forklift returns to yellow
    11. Place a Pallet and drive over it -- forklift passes through (no collision)
    12. Verify arrow keys do NOT scroll the page

    **Combined:**
    13. With forklift, racks, and pallets on canvas, verify capacity counter shows correct total while forklift is moving
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All five phase success criteria verified by human:
1. Running total pallet count updates on element add/remove
2. Total includes floor pallets (stacking), and shelf pallets (rack capacity)
3. Forklift drives with arrow keys
4. Forklift collides with walls/obstacles/racks
5. Visual collision feedback (red highlight)
</verification>

<success_criteria>
Human approves all 13 verification steps above.
</success_criteria>

<output>
After completion, create `.planning/phases/03-capacity-forklift/03-03-SUMMARY.md`
</output>
